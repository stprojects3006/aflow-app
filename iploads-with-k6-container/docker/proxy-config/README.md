# Proxy Configuration Directory

This directory contains proxy configuration files for IP rotation.

## Files
- Configuration files for different proxy types
- Tor configuration files
- Privoxy configuration files
- OpenVPN configuration files

## Usage
The setup-proxy.sh script will create the necessary configuration files at runtime. 